name: Quality Gate
on:
  workflow_call:
    inputs:
      workspace-path:
        required: false
        type: string
        default: '.'
    secrets:
      SIBLING_DEPLOY_KEY:
        required: false

jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - run: echo "step1c test ok"
