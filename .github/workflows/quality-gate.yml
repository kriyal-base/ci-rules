name: Quality Gate
on:
  workflow_call: {}

jobs:
  gate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
    steps:
      - run: echo "step1d test ok"
